{
  "name": "mu-web-tests",
  "private": true,
  "scripts": {
    "test:web:e2e": "cargo build --bin mu && node testing/e2e/web.test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}

{
  "name": "mu-web",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.ts",
    "test:e2e": "cargo build --manifest-path ../Cargo.toml --bin mu && node tests/e2e/web.test.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}

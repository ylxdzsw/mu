# Shell plugins

Shell-specific interactive integrations live here.

- `mu.zsh` — zsh keybinding plugin embedded by `mu init zsh`

Future integrations should live directly in this directory and use a suffix that
identifies the target shell, such as `.bash`, `.fish`, or `.nu`.
